apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: cheonghalim/paranmanzang:gateway
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"  # 활성화할 Spring 프로파일
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config:8888"  # Config Server URL
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
spec:
  selector:
    app: gateway
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: NodePort

